# Markdown TOC MCP Server 依赖包
# 为 TRAE IDE 设计的 Model Context Protocol 服务器

# 核心 MCP 包
mcp>=1.0.0

# 日志和配置
pyyaml>=6.0

# 异步支持
asyncio>=3.4.3

# Markdown 处理依赖
markdown>=3.4.0
markdown-it-py>=2.2.0  # 强大的Markdown解析器，支持完整AST
beautifulsoup4>=4.11.0
lxml>=4.9.0

# 文本处理和分析
regex>=2022.10.0
chardet>=5.0.0

# 性能优化
aiofiles>=22.1.0

# 开发和测试依赖（可选）
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0
coverage>=7.0.0

# 类型提示支持
typing-extensions>=4.0.0